# 外部代理协作 Skill：迭代记录

## 2026-08-03：显式 routing 配置与 CI 控制面补齐

- 将版本控制中的 `.ai-collaboration/providers.shared.json` 显式写入 `routing.default.strategy=fair_round_robin`；保持既有行为，同时使当前生效配置真正由可编辑策略字段表达。
- `test_routing_config.py` 现在同时校验共享配置和公开示例的 provider/profile/routing schema，防止示例与当前共享配置漂移。
- macOS Python 3.11 通过统一 33 脚本回归、编译检查和示例 Goal contract 校验；workflow 增加无凭据的 `bootstrap --init/--check` 与 `doctor.py --routing --json` 控制面检查。
- Windows 实际执行和轮换凭据后的真实 Provider smoke 仍是独立的外部验收门槛，未将本地结果替代为双平台完成证据。

## 2026-08-03：配置化路由最小真实 smoke 复核

- 脱敏 `doctor.py` 检查 DeepSeek/MiMo profile 均通过；首次 fixed-MiMo smoke 在 provider CLI 启动前因本机 trust 记录的平台身份不匹配而 fail-closed，验证了跨平台 trust 隔离。
- 刷新当前 macOS 的非密钥 trust 指纹后，fixed 路由已通过本地 readiness 门并进入实际只读调用；该调用在 180 秒窗口内没有产生终态，随后被停止，不能计为真实 smoke 通过，也没有据此更新专题验收状态。
- 并行刷新 trust 记录时发现固定 `.tmp` 文件名会发生写入冲突；`collaborate.py` 已改为使用同目录唯一临时文件再原子替换，避免该类本地状态写入异常。凭据值未写入代码、文档或记录。
- 针对真实调用无终态时的收敛风险，新增 `process_support.py`：Claude Code/Antigravity adapter 使用独立进程组和有限超时清理；新增 fake timeout 回归，macOS 通过，Windows 证据仍待 CI/主机。

## 2026-08-03：Goal 生命周期完成性规范化

- 根据文档审计结果，明确区分单次 Run 与跨多轮 Goal：`completed` 只代表本轮 outcomes、范围和验证通过，不等于 Goal `achieved`。
- 新增 Goal contract、required criteria、evidence、completion/stop policy、`active/achieved/blocked/failed/cancelled` 状态和合法终态规则；缺失条件、证据、审查或用户验收时不得关闭。
- 同步更新 PRD、技术方案、实施计划、协作协议、expected outcomes、协作效能方案、Skill README、测试用例和 DEC；新增 TC-84 至 TC-93，标记为 Goal runtime 实现后的待验收用例。
- 后续实施已新增 `goal_lifecycle.py`、`collaborate.py --goal-contract`、`.ai-collaboration/goals/` 持久化、统一 `run_regression.py` 和 macOS/Windows CI 矩阵；macOS 全量 `test_*.py`、编译检查和示例 contract 校验通过，Windows CI/主机运行验证仍待执行。

## 2026-08-03：配置化 Provider 路由设计与运行态清理

- 新增专题文档 [可配置 Provider 路由技术设计与实施计划](专题/2026-07-29-provider-routing-failover/可配置Provider路由技术设计与实施计划.md)，定义 `fair_round_robin`、`fixed`、`weighted_round_robin`、配置合并、运行态 cursor、health/failover 边界、兼容迁移、测试矩阵和 macOS/Windows 实施顺序；随后已完成 P0-P3 runtime 实施，Windows CI/主机验证仍待执行。
- 新增 routing loader/validator、三种策略与 state 兼容、主 runner/batch worker 集成、`doctor.py --routing` 和 33 脚本统一回归入口；macOS 全量回归通过，未把一次真实 provider 调用或未执行的 Windows 证据标记为完成。
- 明确 `docs/` 是版本控制的项目事实与规范；`.ai-collaboration/` 中的项目背景、决策、topic/session 控制状态和本地 profile 继续保留，handoff、output、log、snapshot、batch、review 属于可重建 runtime cache。
- 将 15 个已完成且在 2026-08-01 前最后使用的本地 session 归档，保留非敏感 session 索引；使用 `maintain_runtime.py --days 1 --apply` 清理 1,837 个旧 runtime 文件，约 9.5 MB snapshot 及相关 handoff/output/log/batch 缓存。
- 清理后保留 provider local profile、trust、health、capability、metrics、sessions、topics、project context、decisions 与 archive；删除已失效的 topic evidence 引用，避免指向已清理的 output。
- 清理规则使用现有 Python maintenance script，不依赖 POSIX 删除语义；macOS/Windows 的静态状态边界不变，未发起真实 provider 调用。

## 2026-07-28：首版建立与真实验证

### 建立

- 将五轮前置讨论归档至 `history/chatgpt-conversations/`。
- 创建项目级 `external-agent-collaboration` Skill、项目 `AGENTS.md`、协作状态目录和四份设计文档。
- 采用 Codex 为统一入口、MiMo/DeepSeek 为持续协作者、Claude Code CLI 为本地 harness 的协作结构。
- 使用独立 `CLAUDE_CONFIG_DIR`，不通过 CC Switch 的全局当前 provider 路由。

### 配置决策

- 用户明确授权将两家 API token 保存在 gitignored 的 `.ai-collaboration/providers.local.json`。
- token 仅用于对应 API endpoint 的认证；不得进入 Git、文档、交接包、输出或日志。
- 实现时不得读取 CC Switch 数据库或复制默认 `~/.claude` 配置。

### 真实验证与发现

- MiMo 与 DeepSeek 均通过创建 session、精确 `--resume`、受控文件编辑 smoke test。
- 初次 MiMo session 未获得 `Write`，原因是该 session 在执行器添加 `Write` 前创建；恢复时保留其初始工具集。
- 新建的 MiMo ephemeral session 与 DeepSeek ephemeral session 均已实测支持原生 `Write`。
- 对旧 MiMo session，精确 `touch <允许路径>` 加 Edit 的 Bash 兜底成功；不需要开放任意 Bash。这是通用 session 陈旧兜底的验证样本，不是 MiMo 特例或硬编码。

### 能力探测迭代

- 新增 `probe_capabilities.py` 和 `.ai-collaboration/provider-capabilities.json`。
- 探测使用 ephemeral session 和 `capability-lab/`，以实际文件结果判断能力。
- 探测触发条件：首次需要新建文件/目录、七天过期、CLI/profile 变化或工具缺失失败。
- 将 provider 新 session 能力和持续 session 的 `initial_toolset` 分开保存，避免错误路由。

### 实施计划校正

- 审计五轮讨论后，确认当前仅完成核心 harness，不等同于完整协作系统。
- 将实施计划重写为以任务分类、学习路由、session fork/归档、结果完成性验证、双协作者编排、非代码边界和可观测性全部落地为完成条件。
- 下一项实施前置条件确定为：让执行器机器验证 `expected_outcomes`，防止模型仅凭文本报告成功。

### Expected outcomes 实施

- 新增 execute 必填的 JSON outcomes 契约，支持文件存在/内容、变更数量、受限验证命令和受限 JSON Schema。
- 执行器不再以模型最终文本作为成功依据；任一 outcome 失败会恢复该次任务变更并标记 failed。
- 完成了未传 outcomes 的调用前拒绝、本地失败回滚以及 MiMo 真实成功路径验证。

### 自动分类、学习路由与生命周期实施

- 新增确定性的 `classify_task.py`：对敏感、实时/连接器、原生成品、简单任务和复杂本地协作给出可复现的 delegation 决策。
- 新增 `provider-metrics.json` 与 `provider_routing.py`：不保存提示词或文件正文；冷启动按 task type/mode 轮换，达到最小样本后按质量、完成率和耗时选择。
- `collaborate.py` 不再用“现有 session 数量”选择新主题 provider，并将每次调用的匿名结果元数据自动入账。
- 调用账本同时记录 CLI 返回成本；`assess_run.py` 可在 Codex 评估或用户采纳后按 run ID 回写质量分、采纳和返工次数，且不保存理由或内容正文。
- 新增 `topics.json` 自动登记、新 session fork 和 `archive_session.py`。使用 MiMo 的真实只读 smoke 已验证 `--resume <id> --fork-session` 会获得不同 session ID；测试 fork 随后已归档。
- execute 若 outcomes 指向尚不存在的文件，执行器会自动检查/更新能力档案；恢复 session 缺少 Write 时自动 fork，原生 Write 实测不可用才使用精确 Bash 创建兜底。
- 已通过 `test_classify_task.py`、`test_provider_routing.py`、`test_session_lifecycle.py`、`test_outcomes.py` 与 Python 语法校验。

### 一次性独立审查与非代码边界

- 新增 `review_execution.py`：仅对 completed execute 发起另一 provider 的一次只读 critique，输出落盘后自动停止，不会回调执行者。
- 新增按代码/文档研究/创作/数据/个人建议划分的交接模板；明确最终富文本办公文档、图片、表格、演示、PDF、当前信息和连接器数据留在 Codex 原生路径。
- `test_review_execution.py` 已通过。

### 协作效能优化方案

- 审计结果确认：现有外部协作可以让 Codex 不读取大本地材料，但 `collaborate.py` 仍会把完整 provider result 打印回调用端，缺少返回长度和批处理协议。
- 将原 Token 方案重构为 `docs/专题/2026-07-29-协作效能优化/方案.md`：以可验证交付质量为中心，统一规划紧凑回传、持续上下文、风险复核、批处理、路由、总成本与完成效率。
- 方案新增最小协作持久化层：项目产物为事实来源；只长期保留稳定背景、决策、短 topic 状态和本地 session 映射；handoff、输出、日志和快照为带保留期的 runtime cache。
- 10,000 × 1 MB 的场景不得单次 execute；现有全项目快照上限会拒绝，应使用只读 batch 与小范围写入批次。

## 2026-07-29：已登记 provider 的本地外发信任门

- 新增 ignored 的 `trusted-providers.local.json` 与 `trust_provider.py`：用户在本机明确批准 provider 后，runner 才会启动 Claude Code；记录只保存 provider key、批准状态和当前非密钥 profile fingerprint，不保存 token、endpoint 或 handoff 内容。
- profile 的 endpoint、模型映射、配置目录或非密钥 environment 变更会使信任记录自动失效。该门区分本机 Claude Code harness 与已批准的 provider egress，但不绕过 Codex 宿主平台的最终审批。
- 现有 `deepseek` 与 `mimo` profile 已按用户本轮明确指示写入本机信任记录；没有发起 provider 请求。
- `dontAsk` 的 `permission_denials` 现在返回 `blocked_by_permission`，不再可能被无 outcome 的只读调用静默报告为 completed。
- 新增 provider trust 回归、bootstrap 信任记录初始化、Python 编译与全量本地测试；未新增真实 provider smoke。

## 2026-07-29：Provider 路由、故障切换与模型职责实施

- 新增专题入口、方案和 [DEC-20260729-provider-routing-failover](决策记录/DEC-20260729-provider-routing-failover.md)，记录“Codex 选 provider、Claude Code profile 选内部模型”的边界。
- `collaborate.py` 停止传递 `--model`，公开 profile 示例移除废弃的顶层 `model` 字段；CC Switch/Claude Code 的环境映射保持原样注入。
- `provider_routing.py` 改为对所有外部可委派的健康 provider 使用持久化公平轮换；质量、耗时和采纳指标继续记录但不在当前 starter policy 中打破平局。
- 新增 ignored runtime `provider-health.json` 的状态机：billing/authentication/endpoint/configuration 冷却 24 小时，rate-limit/transport/server 按 5 分钟、15 分钟、1 小时、6 小时退避；仅已归类的 availability failure 才能最多切换另一健康 provider 一次。任务、契约、outcome 和范围失败不会触发切换。
- 新增本地 `test_provider_health.py`，覆盖冷却、活跃 session 的新 provider 会话切换、失败分类与 command 不含 `--model`；未运行新的真实 provider smoke。

## 2026-07-29：协作效能与文档治理决策

- 将 Token、质量、效率、批处理、独立审查和最小协作持久化统一为 `docs/专题/2026-07-29-协作效能优化/方案.md`，而非并列的单指标优化。
- 确认项目产物是事实唯一来源；长期协作状态仅保留稳定背景、确认决策、短 topic 状态和本机 session 映射；运行日志、完整输出、handoff 与快照为可清理 runtime cache。
- 确认文档采用“持续更新的主文档 + 版本化决策记录 + Git 历史”的混合治理：主文档描述当前规则，重大取舍写 DEC，迭代记录写实际变更和验证。
- 同步更新 PRD、技术方案、实施计划和测试用例；协作效能 P0/P1/P2 当时仍为待实施工作，不应描述为已实现。


### 专题归档与可追溯性

- 将原顶层 `docs/协作效能优化方案.md` 迁入 `docs/专题/2026-07-29-协作效能优化/方案.md`，并新增专题入口和专题索引。
- 专题入口固定记录讨论时的实现基线（`14a8985`）、P0/P1/P2 当前状态、关联 DEC、主文档映射及未来实施证据要求；方案本身不再被误读为当前实现状态。
- 新增 `docs/README.md`，明确顶层规范、专题、DEC 与迭代记录的职责边界。


## 2026-07-29：协作效能 P0 实施

- `collaborate.py` 新增默认 `compact`（最多 8 KiB）、`structured`、`file_only` 与显式 `debug` 回传模式；完整 CLI JSON 只写入 ignored local output。
- 新增受限 response contract、契约失配记录、默认回传的常见 token 形态脱敏，以及不取代 execute machine outcomes 的状态判定。
- `bootstrap.py` 初始化最小本地协作状态；每个非 ephemeral run 写入一页 topic state，不保存完整 handoff、聊天或 provider output。
- 本地 `test_return_modes.py`、`test_bootstrap.py` 通过；一次已配置 provider 的只读 structured smoke 返回有效 contract，且无项目文件变更。
- P1 batch 和 P2 对照路由尚未实施。

## 2026-07-29：协作效能 P1–P3 实施

- 完成只读 batch worker、真实小样本、JSON fence 兼容和 reducer 验收。
- 增加 P2 效能策略、分类效率字段与非敏感指标字段。
- 增加 P3 runtime 保留期维护与保守模式健康信号。

## 2026-07-29：真实文档分析回归优化

- 修复孤立英文 `current` 被误判为实时信息的问题。
- structured response 支持严格最外层 JSON fence；可恢复结果不再触发同一 consult 的自动重复调用。
- Skill 要求对 provider 的链接/文件/测试覆盖声明进行本地验证，不将模型自述当作证据。

## 2026-07-29：Windows 兼容性 P0/P1 实施

- 新增 `platform_support.py`，集中判断主机平台、规范化工作目录、生成不暴露用户路径的工作区 identity，并拒绝不属于当前 host platform 的 session/capability 记录。
- `collaborate.py` 新 session 写入 `host_platform` 与 `workspace_identity`；显式或自动恢复均不再跨平台匹配。`migrate_runtime.py` 支持默认只读报告和显式 `--apply` 隔离 foreign active session，应用前备份 ignored runtime state。
- `command_succeeds` 新增优先的 `argv` / `--validation-argv` 路径，以 `shell=False` 执行；原 string `--validation-command` 仅作为兼容模式保留。Windows 不再依赖 Unix `true` 作为回归成功命令。
- macOS Keychain 分支改为仅在 macOS 上运行；Windows 等非 Darwin 主机返回脱敏、可操作的认证诊断。bootstrap 在 Windows 不再将 POSIX `chmod` 当成 ACL 成功证据。
- capability probe 记录 host platform 和 shell evidence；Windows 不会运行 `touch` Bash 兜底，也不会复用 macOS capability 结论。
- 新增平台、迁移和诊断回归，并修复 provider-health fixture 的硬编码 POSIX 工作目录与 Windows fake launcher。所有本地回归在 Windows 通过；未运行真实 provider 调用。

## 2026-07-29：双平台可同步 profile P2 控制面

- 新增 `profile_support.py` 与 tracked 的 `providers.shared.json`：共享层使用 `config_dir_relative_to_home`、逻辑 `claude` launcher 和 `auth_token_env`，按 shared → 平台 override → local → 环境合并；Windows/macOS 分别解析到各自主目录。
- `collaborate.py`、`doctor.py` 与 `probe_capabilities.py` 统一使用该加载层；保留旧扁平 `providers.local.json` 的本机兼容，但 shared 配置出现 token、Keychain、绝对目录或 `.exe` launcher 时会在调用前拒绝。
- 信任 fingerprint 现包含认证方式标识；认证值与敏感 environment 值仍被脱敏。新增 shared profile、诊断、bootstrap 与信任回归；17 项本地测试均通过，未发起真实 provider 调用。
- 未自动迁移用户现有 token：将 macOS 的旧 local profile 改为可同步 portable profile 前，仍需用户在 macOS/Windows 分别配置同名认证环境变量或各自的 OS 凭据存储，并明确重新批准信任记录。

## 2026-07-29：Windows provider 凭据迁移

- 按用户明确授权，`migrate_portable_profiles.py --apply` 已将当前 Windows 配置中的两个 provider token 写入当前用户环境变量，并生成 credential-free shared profile、Windows platform overlay 与隔离配置目录；命令输出未包含 token。
- 迁移工具新增 macOS Keychain 路径与双平台完成后才可执行的 `--cleanup-legacy`。legacy 清理会先在用户目录建立非同步备份；macOS 侧迁移尚待在该主机执行，因此本轮未清理同步旧配置中的直接 token，也未发起 provider 调用。

## 2026-07-30：配置文件直接 token 原则（取代系统凭据迁移）

- 用户明确确认：Git 忽略的配置文件是本项目默认且唯一要求的 token 载体。可维护通用 `providers.local.json`，或分别维护/私有同步 `providers.local.macos.json` 与 `providers.local.windows.json`；直接 `auth_token` 是受支持配置。
- 环境变量、macOS Keychain、Windows Credential Manager 及其他系统凭据库均不再是默认、推荐或自动迁移目标。2026-07-29 的 Windows 环境变量迁移记录仅保留为历史事实，已由本决定废止。
- 文档、公开示例、平台 profile 测试和迁移器均改为配置文件优先；token 仍严禁进入 Git、handoff、输出、日志、测试 fixture 或外部提示词。
- 新增 [Provider 配置文件与 macOS 迁移交接专题](专题/2026-07-30-provider-config-files/README.md)，记录 Windows 已完成状态、macOS 无 Keychain/环境变量迁移步骤与不触发真实 provider 的停点。

## 2026-07-31：macOS provider profile 迁移接手完成

- 已执行本地 `migrate_portable_profiles.py --apply`，将 DeepSeek 与 MiMo 的旧通用 local profile 信息迁移至 macOS 平台 local profile，并初始化隔离配置目录；未读取、显示或写入 token 到受版本控制文件。
- 两个 provider 的本地 `doctor.py --json` 都返回 `ok: true`；runtime 迁移只读检查未发现 foreign session。18 项本地 Python 回归、脚本编译与 `git diff --check` 均通过。
- 为使两端测试真正可移植，修正了 Windows 路径断言、会过期的健康状态时间戳，以及把 macOS 路径误当 foreign-host 的 session fixture。
- 未执行真实 provider/Claude Code 调用，未清理旧通用 profile；平台生效配置的 fingerprint 已变化，实际调用仍需用户明确重新批准该 macOS provider 信任记录。

## 2026-07-31：真实 provider smoke 与旧通用 profile 清理

- 经用户明确授权，已为当前 macOS 生效 profile 重建 DeepSeek、MiMo 的本地非密钥信任 fingerprint。真实 read-only `consult` 由公平轮换自动选中 DeepSeek；runner 未传 `--model`，无工具调用、权限拒绝或项目文件变更，provider health 保持 healthy。
- 此次 CC Switch/Claude Code profile 实际记录了 DeepSeek Flash 与 Pro 的内部用量，符合“Codex 只路由 provider、profile 决定内部别名和模型”的边界。核心 smoke 短语虽已返回，但额外附带解释和 JSON，故 response contract 记录为不严格匹配；按协议未自动重试。
- 已执行 `migrate_portable_profiles.py --cleanup-legacy`，旧通用 `providers.local.json` 的直接 token 已清空，并在用户私有目录创建备份；状态检查确认 macOS/Windows platform overlay 均存在，legacy direct token provider 列表为空。

## 2026-07-31：Headless CLI 参考基线与多 harness 规划

- 新增 `docs/headless-cli-references/`，保存 Claude Code 与 Antigravity 官方 headless 页面的 Markdown 转换稿及其参考说明。它们是调用、权限、schema、会话和跨平台设计的重要输入；具体 flag 仍须以官方原文和本机 CLI 为准。
- 当前有效规范、README、Agent/Skill 规则与协作协议均增加“每次迭代同时考虑 macOS 与 Windows”的强制要求：记录平台影响，完成双端验证或提供具体不受影响理由。
- 新增 DEC 和专题，确定 Claude Code 与 Antigravity 是独立 harness；先优化 Claude 原生 JSON Schema 输出，再做 adapter/state 迁移和 Antigravity read-only 验证，最后才讨论 execute 或自动候选池。未实现 Antigravity adapter，本专题也未发起任何 Antigravity CLI 调用。
- 本轮受控外部架构咨询因分类器将 handoff 误判为 `prohibited` 而未调用 provider；未绕过安全门，规划由 Codex 本地完成。
- 根据后续澄清，已将 Antigravity 的 P2 后默认自动角色收窄为无既有会话的只读独立评审（第二方案、反证、风险清单）；项目续接、受限执行与普通新项目协作仍由 Claude Code 承担，只有其 DeepSeek/MiMo provider 池执行公平轮换。并将“安全政策中出现 token/secret 等词”误判为敏感的修复列为实施 P0 前置。
- 已实施 WP-11.0/WP-11.1：新增 value-aware `sensitivity.py`，安全政策文字不再被 substring 误判；真实 credential value/assignment、私钥、`.env` 正文及明确客户/生产数据外发仍被拒绝，疑似引用要求先 redaction。runner 在启动 CLI 前独立复扫 handoff，且错误不回显值。
- 新增 `ClaudeCodeAdapter`：保留 `dontAsk`、精确工具白名单、隔离 profile、session resume/fork 与“不传 `--model`”，并在标准调用传入 `--json-schema`、优先读取 `structured_output`；仅该字段缺失时回退旧 `result` JSON。macOS 全套 Python 回归、fake launcher、编译与 diff 检查通过；Windows fake `.cmd` 分支已覆盖，但尚未在 Windows 主机实际执行。未调用 provider 或 Antigravity。
- 继续完成 WP-11.2 的 Claude-first 基础：新增 `HarnessAdapter` 协议和 runtime identity；新 Claude session、trust、health、capability、metric event 写入 harness/profile/platform identity，旧 record 由 `migrate_harness_state.py` 默认 dry-run、显式 `--apply` 备份后标注。`EXTERNAL_AGENT_HARNESS_STATE=1` 启用后 foreign-harness active session fail-closed；默认 Claude 路由不变。已覆盖 dry-run、apply backup、legacy 兼容与 foreign-session 拒绝；尚未实现或调用 Antigravity。
- 已实现 WP-11.3 的代码与 fake 回归：本机 `agy --help` 核对 JSON Schema、conversation、plan 模式与危险 skip flag 后，新增 read-only `AntigravityAdapter`、`consult_antigravity.py`、非密钥 harness profile 和本地 fingerprint trust。它只接受 consult/critique、固定 plan，soft denial 即使 exit 0 也阻断；不会自动路由、不会调用 `--dangerously-skip-permissions`。未进行真实 `agy -p` 请求，仍等待交互认证、profile approval 与双平台 smoke。
- 在用户批准 `antigravity_readonly` profile 后，已创建其本地 trust record，并尝试无工具、无敏感的 P2 schema smoke。`agy` 在产生可用 response/conversation 前以 exit 1 失败，未生成项目变更或 session；由于 stderr 不安全，不在日志或用户输出中回显。增强 runner 仅输出 exit code、terminal status 和脱敏错误类别；当前失败未能可靠归类，真实 smoke 仍未通过，需先完成/确认交互式 `agy` 登录后再试。
- 用户确认交互登录后，手动最小 `agy -p` 成功，定位前述失败来自 Codex sandbox：它拒绝 agy 写用户日志与绑定 localhost，不是 OAuth、model 或 schema 问题。在允许本机运行态的执行环境中重跑已批准 smoke，获得 completed、有效 schema contract、独立 Antigravity conversation/session 与 `permission_state=allowed`；未产生项目文件变更。Windows 仍未执行 fake/真实 smoke。

## 2026-07-31：连续本机执行与 Claude stream/schema smoke

- `AGENTS.md`、中文镜像与 Skill 双语说明现在记录维护者的持续本机执行授权：迭代不再为常规诊断、测试、non-sensitive real smoke 或本地 trust 指纹刷新而询问冗余确认；仅在必须人工完成登录/MFA/CAPTCHA/OS 对话、缺少秘密值、目标真实不明或宿主平台本身拒绝时暂停。任何 GitHub 推送前仍扫描并排除真实 token/凭据；平台自身的 egress 审批不能由项目文件绕过。
- 新增 `stream_diagnostics.py`，并接入 `ClaudeCodeAdapter` 的 opt-in `--stream-diagnostics`：只记录 event type 计数、启动、retry、plugin/MCP failure、权限信号与终态，不保存 prompt、模型文本、完整 event 或子代理文本。`AntigravityAdapter` 同时具备 `stream-json` terminal/lifecycle 解析；两端 fake launcher 回归通过。
- 使用受信任的 DeepSeek Claude Code profile 完成无敏感、无工具的真实 `stream-json + --json-schema` smoke，并以保存的 session ID 成功 `--resume`；两次均返回有效 `structured_output`，未修改项目文件。以 MiMo profile/隔离临时 Claude config 依次尝试 `pwd` 与 `AskUserQuestion` permission smoke 时，当前 CLI/model 均未实际发起工具调用，未产生可观察的 `permission_denials`；因此没有把它误报为拒绝。`permission_denials -> blocked_by_permission` 仍有 fake launcher 回归覆盖。
- 全部本地 Python 回归、针对 adapter 的 bytecode compile 以及 `git diff --check` 已通过。P2 的 Windows fake/live smoke 仍待 Windows 真机；被双平台门槛阻止的 Antigravity execute 与自动角色路由尚未启动。

## 2026-07-31：Windows Codex 接手入口

- 新增版本控制的 [Windows Codex 接手文档](专题/2026-07-31-headless-multiharness/WINDOWS_HANDOFF.md)，取代三个仅供早期 P0/P1 使用、已过期的 ignored runtime handoff。它给出 Windows bootstrap/doctor/trust、完整 PowerShell 回归、Claude schema + exact resume、Antigravity P2 read-only smoke、验收结果和 P3/P4 解锁条件。
- 专题中同时新增两份无敏感、无工具的 tracked smoke handoff，并从专题目录索引和专题入口链接；Windows Codex 无需依赖 macOS session、token 或本对话即可接手。`git diff --check` 与 sensitivity 回归通过。

## 2026-08-01：Windows 双运行器验收与精确回应烟测

- Windows 两个 Claude provider 与 `antigravity_readonly` harness 的本地诊断均通过；已刷新当前非密钥信任指纹。
- Windows 全量 Python 回归通过（23 项），覆盖 platform/session isolation、Claude Code adapter、Antigravity adapter、harness profile/trust 与 response contract；编译和 diff 检查另行通过。
- DeepSeek 的无工具 Claude Code schema smoke 及其同会话 `--resume` 均为 completed，第二次路由为 `explicit_session_key`，两次均无项目文件变更。
- Antigravity P2 的无工具、plan-mode schema smoke 为 completed，保留独立 harness conversation/session，未使用 `--dangerously-skip-permissions`，无项目文件变更。
- 针对 macOS 曾发现的“精确短语与默认 JSON contract 冲突”，新增受限 `--response-contract none --expected-response`：仅允许 ephemeral consult/critique 的单行精确回应检查，不传 JSON schema；错误期望文本会失败且不触发 provider failover，正确短语的 Windows 真实 smoke 已通过且无文件变更。
- P2 现具备 macOS/Windows 本地回归与真实无工具 smoke 证据；Antigravity execute（P3）和自动候选路由（P4）仍未启动。

## 2026-08-01：本机真实验证默认执行

- 纠正了“真实 smoke 必须逐次取得对话授权”的过时规则。对于已经配置、当前非密钥 fingerprint 已信任的 Claude Code 或 Antigravity 本机 profile，最小、无敏感的真实调用是正常工程验证，Codex 应直接执行并据可观察结果继续诊断。
- `AGENTS.md` 明确禁止在优化本 Skill 时自行加入“完全不调用 provider”的暂停；文件写入调用仍需最小且可回滚的明确范围，凭据/私有数据不得外发，宿主平台 egress 审批仍是唯一额外边界。
- 同步更新测试用例、技术方案、实施计划、DEC 和 multi-harness 专题。macOS：可直接继续 P3 对照 smoke；Windows：保留已记录的 P3 故障并以同一最小契约复测，不因无逐次授权而停顿。
- 随后真实运行 macOS P3 对照：基础 `plan` PING 成功，`antigravity_execute` profile doctor/trust 通过；但同一预先存在、单文件、无 shell、可回滚 smoke 与 Windows 一样得到有效结构化响应、零文件变更和 outcome failure（run `1785565844-ef7bfc83`）。因此 P3 故障不是 Windows 独有；已将下一步收敛为无内容的 stream-json 工具调用诊断，而不是放宽权限或使用危险跳过参数。

## 2026-08-01：开放的本机 harness 开发边界

- 将维护者的非技术化授权转写为 `AGENTS.md` 的可执行规则：本机已配置 Claude Code/Antigravity、其 cached login 和 Git 忽略的明文 local profile 都属于已授权开发环境；正常真实调用与本机 CLI 日志、localhost helper、配额消耗或读取本地 profile 不构成暂停理由。
- 明确定义唯一相关的 disclosure：不得把实际凭据值、`.env`/local-profile 正文或无关私有数据复制进版本控制、Git 外发、handoff/prompt、共享日志或用户输出；这不限制本机 CLI 使用其自身配置完成任务。只有真实宿主/OS 强制拒绝才是 platform boundary，须直接请求并在获准后继续。

## 2026-08-01：AGY full-auto P3 结论

- 实现 `local-full-auto` profile、无内容 stream diagnostics 与 disposable isolated executor；主工作树禁止 full-auto，避免 AGY 越界尝试与本地改动并发冲突。
- 真实 macOS 对照证明：`--dangerously-skip-permissions` 已使 effective mode 成为 `always-proceed`，且 `write_to_file` 可用；但无论主工作树还是只含一个目标文件的隔离目录，AGY 都不写目标。故障不是 settings、Windows 或 workspace 范围问题。
- 当前产品结论：AGY 保持 P2 只读独立协同；Claude Code 是唯一自动 execute harness。未来仅在 AGY CLI/agent 更新后以同一隔离 P3 契约重新验证。profile/adapter 回归、Python 编译和完整本地测试集均通过。

## 2026-08-01：现行文档一致性审计

- 对 README（中英文）、四份主文档、两份相关 DEC、专题索引、multi-harness 专题及 Windows 接手文档进行了现行规则审计；历史实验记录保留，但明确标注已被 isolated full-auto 证据取代的中间假设。
- 统一当前状态：Claude Code 是唯一自动项目 execute harness；DeepSeek/MiMo 仅在 Claude Code pool 内公平轮换；Antigravity 仅承担已完成双平台 P2 验证的只读独立评审，不因新主题自动选中，也不进入 execute 路由。
- 统一维护者授权表述：configured、fingerprinted trusted 的本机 profile 可直接运行最小非敏感验证；profile 变化后的非密钥 trust record 在已授权实施中自动刷新。只有网页登录/MFA 等确实需要人工的交互或宿主平台强制边界才暂停。
- 同步 P3 结论：macOS/Windows controlled evidence 与 macOS isolated full-auto evidence 均不支持 AGY 自动写入；不再尝试以 settings 或路径 allowlist 修复，未来只在 CLI/agent 更新后复验同一 isolated 契约。

## 2026-08-01：Windows 接手证据对齐

- 更新 Windows 接手文档，明确 Windows 已完成 P0/P1/P2 及 P3 controlled evidence；与 macOS 当前进度唯一不对称项是一次 disposable isolated full-auto 对照。
- 该对照使用 Git 忽略的 fresh `P3 pending` source，避免历史 tracked artefact 已含目标文本造成假阳性；其结果只补齐平台证据，不能自动解锁 AGY execute。
- 同步实施计划与测试用例：若当前 Windows 仍不写目标，继续固定 AGY 为只读；若意外成功，须先由 macOS 同契约重现、通过完整回归并新增 DEC，才可重开 execute 评估。

## 2026-08-01：Windows isolated full-auto P3 对照完成

- Windows 运行全部本地 Python 回归、相关语法检查与 `git diff --check` 均通过；本机 `agy` 为 `1.1.9`，其已安装帮助与官方 headless 文档均确认 `--mode accept-edits`、`--output-format stream-json`、`--json-schema` 和 `--dangerously-skip-permissions` 的隔离实验语义。
- 首次 isolated run 在 180 秒时被 Python 外层超时截断，未构成能力证据。为避免 CLI 的 `--print-timeout` 与 Python 进程终止竞争，`AntigravityAdapter` 现保留 15 秒收尾余量；该行为已有 Windows/macOS fake launcher 回归。
- 重跑 run `isolated-1785572668-1f49fab1`：CLI exit code `0`、terminal `SUCCESS`、`permission_mode=always-proceed`、`write_tool_available=true`；20 个工具步骤中没有写入步骤，唯一 pending 目标未匹配，且没有非目标变更。Windows 由此与 macOS isolated full-auto 证据对齐。
- 当前 Skill 可正式使用：Claude Code 是唯一自动 execute harness；AGY 只用于已验证的 P2 read-only 独立评审。未来只有 AGY CLI/agent 更新且同一 isolated P3 契约在 macOS 和 Windows 均成功后，才可重新评估 execute 路由。

## 2026-08-01：P4 公共只读角色路由实施

- 新增 `route_harness.py` 作为 Skill 的公共通用入口；它不会改变旧 `collaborate.py` 的兼容行为。router 先按精确 session、用户指定、handoff 敏感性、请求中的独立审查意图、当前 profile/trust/launcher readiness 与 workspace/platform identity 选择 harness，再只在 Claude Code 内执行 DeepSeek/MiMo provider routing。
- ready Antigravity 只会处理新/无历史、无敏感、明确独立审查/第二方案/反方意见/风险审查的 `consult` 或 `critique`；已有 Antigravity session 仅能 read-only continue。execute、draft、命令、outcome、fork 与 full-auto profile 均在 router 前拒绝。每个结果 record 写入 harness 和 routing basis。
- Windows P4 real smoke run `1785589528-4586c0af` 已通过：public router 自动选择 Antigravity，`routing.basis=explicit_independent_review`、结构化 contract 有效且项目无变更。完整 Python 回归、语法检查与 diff 检查通过。
- 已新增 [macOS P4 handoff](专题/2026-07-31-headless-multiharness/MACOS_P4_HANDOFF.md)。macOS 需按同一 read-only smoke 取得实机证据；这不阻塞 Windows 的当前使用，也不影响“Claude Code 是唯一自动 execute harness”的产品结论。

## 2026-08-01：P4 macOS 自动路由验收

- macOS 的 P4 public-router smoke 已通过：run `1785590735-1988157e` 自动选择 `harness=antigravity`、`routing.basis=explicit_independent_review`，结构化 contract 有效，且没有项目文件变更；P4 现取得 macOS/Windows 双平台运行态证据。
- 第一次 macOS P4 输入得到未结构化结果，第二次没有形成结果 record；两者均未被误报为成功，也未触发跨 harness fallback。已用与通过的 P2 smoke 一致的 response-contract handoff 在新主题中复验成功。
- `route_harness.py` 同时补强：AGY independent-review profile 未就绪时明确交回 Codex，绝不静默改投 Claude Code；AGY 路径只接受标准 structured contract，拒绝 execute/draft、命令、outcome、fork 与 full-auto profile。

## 2026-08-03：配置化 Provider routing 生产收口与真实 smoke

- 在 `codex/configurable-provider-routing` 分支完成并推送配置化 routing；顶层 `routing` 继续支持 `fair_round_robin`、`fixed`、`weighted_round_robin`，主 runner、batch worker、health/failover 和 doctor 共用同一解析器。新增 profile-level `response_transport`：当前 DeepSeek/MiMo 使用 `buffered_sse`，以短生命周期、loopback、随机路径的内存 relay 兼容 Claude Code 与 provider chunked SSE 的退出 framing；默认 `direct` 行为不变，relay 不保存正文、请求或凭据。
- 发现并修正 MiMo 当前不接受的 `mimo-v2.5-pro[1M]` / `mimo-v2.5[1M]` 标识，shared profile 改为 `mimo-v2.5-pro` / `mimo-v2.5`；同时按 Claude Code headless 认证规则在子进程环境中注入 `ANTHROPIC_API_KEY`，保留 `ANTHROPIC_AUTH_TOKEN` 兼容自定义网关，token 仍只来自 Git 忽略的 local profile。
- 修复 Claude CLI 已写出终态但进程未退出时被误判为 transport timeout；终态错误不再回显原文，并能将 stdout 中的 `Insufficient account balance` 归类为 `billing`，写入 health cooldown。
- macOS 真实 DeepSeek smoke `1785738096-02d58477` 通过：`stream-json` 终态、精确 `ROUTING_SMOKE_OK`、无 retry、无项目文件变更。自动路由 smoke `1785738286-26ba6796` 在 MiMo billing cooldown 后选择健康 DeepSeek，使用配置的 `fair_round_robin`，同样通过且无项目文件变更。
- MiMo 真实 smoke 到达修正后的模型调用链，但 provider 返回账户余额不足；runner 明确记录 `billing`、打开 24 小时 cooldown，不触发错误的成功或无界重试。充值或更换有效余额后，可用同一 handoff 重跑 MiMo smoke；这不是代码或 routing schema 的未实现项。
- 新增 relay、终态归一化、认证别名与 profile schema 回归；macOS 本地统一回归通过，共 34 个测试脚本。GitHub Actions run `30791528761`（[`macos-latest`/`windows-latest`](https://github.com/jiezhengj/External-Agent-Collaboration/actions/runs/30791528761)）均通过。改动同时检查 macOS loopback/进程清理与 Windows `.cmd`/进程组路径，未把 POSIX 命令写入实现；Windows 适配器测试改为不依赖临时 Python 子进程 shim，生产路径仍由跨平台 bounded process 与 relay 测试覆盖。
- Windows Codex 接手文档已按最终基线刷新：代码无需继续修改；Windows 机器只需完成自身 Git 忽略的 provider profile、doctor、trust 和本地回归。Windows 真实 provider smoke 仅在要证明该机器实际外发能力时运行；CI 不使用真实凭据。明确保留 AGY P3 blocked 结论，不把它混入 Provider routing 接手条件。
