# Windows Antigravity P2 smoke

Return the required response-contract object. This is a non-sensitive read-only smoke: do not read or edit project files, run commands, invoke subagents, or access credentials. In `summary`, state exactly that this is a non-sensitive Windows Antigravity schema smoke. Leave all arrays empty and set `uncertainty` to `None.`
