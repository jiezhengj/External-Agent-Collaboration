# Windows Claude Code schema smoke

Return the required response-contract object. Do not read project files, edit files, run commands, invoke subagents, or access credentials. In `summary`, state exactly that this is a non-sensitive Windows schema smoke. Leave all arrays empty and set `uncertainty` to `None.`
