interface:
  display_name: "外部代理协作（仅 Codex）"
  short_description: "仅供 Codex 协调持续外部协作、跨模块实施与独立技术评审"
  default_prompt: "仅由 Codex 使用 $external-agent-collaboration，为本项目的跨模块任务或独立评审协调外部协作者。其它 Agent 不得使用。"

policy:
  allow_implicit_invocation: true
