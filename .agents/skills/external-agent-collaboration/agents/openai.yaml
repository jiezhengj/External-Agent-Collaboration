interface:
  display_name: "外部代理协作"
  short_description: "协调持续外部协作，处理跨模块、多文件实施与独立技术评审"
  default_prompt: "使用 $external-agent-collaboration 为本项目的跨模块任务或独立评审协调外部协作者。"

policy:
  allow_implicit_invocation: true
